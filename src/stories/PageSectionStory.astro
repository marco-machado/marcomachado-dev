---
import StoryCanvas from "./StoryCanvas.astro";
import PageSectionDemo from "./PageSectionDemo.astro";

interface Props {
  title: string;
  num: string;
}

const { title, num } = Astro.props;
---

<StoryCanvas>
  <PageSectionDemo title={title} num={num} />
</StoryCanvas>
