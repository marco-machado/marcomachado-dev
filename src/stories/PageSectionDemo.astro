---
import PageSection from "@/components/PageSection.astro";
import PostRow from "@/components/PostRow.astro";

interface Props {
  title: string;
  num: string;
  spacing?: "default" | "large";
}

const { title, num, spacing = "default" } = Astro.props;
---

<PageSection title={title} num={num} spacing={spacing}>
  <PostRow
    idx={0}
    slug="the-prompt-isnt-the-bottleneck"
    title="The Prompt Isn't the Bottleneck"
    description="A sample row inside a standardized section wrapper."
    date={new Date("2026-03-04T00:00:00Z")}
    tags={["AI", "Agents"]}
    lang="en"
  />
</PageSection>
